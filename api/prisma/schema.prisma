model User {
  id               Int      @id @default(autoincrement())
  email            String   @unique
  password         String

  resetCode        String?  // código para recuperación, opcional
  resetCodeExpires DateTime?  // expiración del código, opcional

  // otros campos que tengas...
}
